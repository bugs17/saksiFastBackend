/** @type {import('next').NextConfig} */
// const nextConfig = {};
const nextConfig = {
    images: {
      domains: ['diskominfo.patikab.go.id'], // Tambahkan domain yang diperlukan
    },
  };

export default nextConfig;
